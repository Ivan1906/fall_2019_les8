import React, { Fragment } from "react";

const Home = () => (
  <Fragment>
    <h1>Welcome to my list todos</h1>
    <p>Add your first todo to list</p>
  </Fragment>
);

export default Home;
